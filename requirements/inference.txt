# EPI Monitor V2 — Inference service (YOLO real-time)
-r base.txt

# ML inference (ONNX — Apache 2.0; ultralytics isolado em training.txt)
torch>=2.0.0
torchvision>=0.15.0
onnxruntime>=1.17.0

# Video
opencv-python-headless>=4.9.0
numpy>=1.26.0
