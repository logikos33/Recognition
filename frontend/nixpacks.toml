[phases.setup]
nixPkgs = ["python3", "nodejs_20", "npm"]

[phases.install]
cmds = ["cd frontend && npm ci"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "python3 frontend/serve.py"
