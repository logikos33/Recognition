[phases.setup]
nixPkgs = ["python3", "nodejs_22", "npm"]

[phases.install]
cmds = ["cd frontend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "python3 frontend/serve.py"
