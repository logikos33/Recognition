[build]
builder = "DOCKERFILE"
dockerfilePath = "pre-annotation-service/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 120
