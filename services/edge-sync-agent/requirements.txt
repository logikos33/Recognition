httpx>=0.27

# Evidence API (task-090 — recorder-first mini-API local, ADR-0045)
# Flask: já é o framework padrão do backend cloud (services/api) — reuso
# consciente em vez de introduzir um segundo framework HTTP no monorepo.
flask>=3.0.0
PyJWT>=2.8.0
cryptography>=42.0.0

pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
