httpx>=0.27

# Evidence API (task-090 — recorder-first mini-API local, ADR-0045)
# Flask: já é o framework padrão do backend cloud (services/api) — reuso
# consciente em vez de introduzir um segundo framework HTTP no monorepo.
flask>=3.0.0
PyJWT>=2.8.0
cryptography>=42.0.0

# Frame collector (task-092/093 — Onda 2 shadow mode): decode + downscaled
# grayscale diff for motion detection. Deliberately NOT opencv-python — ARM/
# Jetson wheels are a known landmine (JetPack ships its own OpenCV build; a
# second pip install risks shadowing/conflicting with it). Pillow has
# prebuilt manylinux ARM64 wheels, no compile-from-source risk on the Jetson.
Pillow>=10.0.0

# Gatilho de coleta por PESSOA (fase 1 — NÃO é a detecção de EPI do produto):
# YOLOX ONNX, Apache 2.0, só a classe person. ZERO ultralytics/AGPL (ADR-0043).
# onnxruntime (CPU) tem wheel manylinux aarch64 pronta pra cp310 — verificado
# no Orin; a wheel do PyPI não traz CUDAExecutionProvider, e está certo assim:
# 25ms/inferência em CPU com poll de 3s não justifica disputar a GPU com o
# live view/DeepStream. numpy entra como dependência do pré/pós-processamento
# (o motion_detector segue Pillow-puro; a restrição documentada ali é a
# opencv-python, não numpy).
# O ARQUIVO DO MODELO NÃO VEM NO REPO — .gitignore linha 45 é política do
# projeto (pesos vão pra R2/download). Caminho por COLLECTOR_PERSON_MODEL_PATH;
# ausente, o coletor degrada pro gatilho por movimento.
onnxruntime>=1.17
numpy>=1.24

# DetectionRelay (detection_relay.py): assina det:* no Redis LOCAL do box
# (relay de detecções, ADR-0002) e enfileira no SQLiteBuffer que o uploader
# drena. Cliente puro-Python, import tardio — só é tocado com EDGE_REDIS_URL.
redis>=5.0

pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
