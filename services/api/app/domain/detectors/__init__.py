"""
Detectors — backends de inferência Apache 2.0 (task-055a / A1).

Substitui ultralytics (AGPL-3.0) no caminho de inferência servido.
Backends disponíveis: yolox_onnx, rfdetr_onnx.
Ultralytics foi removido de vez (task "treino não pode mentir") — não há
mais nenhum requirements.txt no repo que o referencie.
"""
