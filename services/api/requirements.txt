# EPI Monitor V2 — Production Dependencies
# Pinned versions for reproducible builds

# Web framework
flask>=3.0.0,<4.0
flask-socketio>=5.3.0,<6.0
flask-jwt-extended>=4.6.0,<5.0
flask-cors>=4.0.0,<5.0

# ASGI/WSGI
gunicorn>=21.0.0,<23.0
eventlet>=0.35.0,<1.0

# Database (psycopg2 direto — NUNCA SQLAlchemy)
psycopg2-binary>=2.9.0,<3.0

# Task queue
celery[redis]>=5.3.0,<6.0

# Cache / Message broker
redis>=5.0.0,<6.0

# Object storage (Cloudflare R2 via S3 API)
boto3>=1.34.0,<2.0

# Security
bcrypt>=4.1.0,<5.0
cryptography>=42.0.0,<44.0
PyJWT>=2.8.0,<3.0

# Validation
pydantic>=2.5.0,<3.0

# Computer Vision (frame extraction/quality — YOLO está no inference-service)
opencv-python-headless>=4.9.0,<5.0
Pillow>=10.0.0,<11.0
numpy>=1.26.0,<2.0

# Frame quality (perceptual hash for dedup)
imagehash>=4.3.0,<5.0

# SSH (Vast.ai training dispatch)
paramiko>=3.4.0,<4.0

# Logging
structlog>=24.0.0,<25.0

# API Documentation
flasgger>=0.9.7,<1.0

# Utilities
python-dotenv>=1.0.0,<2.0
requests>=2.31.0,<3.0
openpyxl>=3.1.0,<4.0

# AI verification agent
anthropic>=0.34.0,<1.0

# Observability (OPT-IN via SENTRY_DSN env var — no-op sem DSN)
sentry-sdk>=2.0.0,<3.0
