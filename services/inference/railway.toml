[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
rootDirectory = "services/inference"

[deploy]
startCommand = "python -m inference.main"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
