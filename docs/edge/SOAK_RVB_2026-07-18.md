# SOAK RVB — task-113 — Co-residência da stack completa no Orin NX

> **STATUS para o Vitor (chegou da estrada):** leia a seção **VEREDITO** primeiro.
> Box: `ssh pandora@100.93.126.76`. Workspace: `~/soak113/`. Soak iniciado **2026-07-18 03:15Z**.
> Execução autônoma, sem sudo (ver pendências). Harness versionado em `deployments/edge/soak113/`.

> 🔴 **BLOQUEIO DE SEGURANÇA PRÉ-CUTOVER (aberto em 2026-07-18):** a senha do admin
> `admin@rvb.com.br` foi **commitada no git durante o embarque** (via `RVB_ADMIN_PASSWORD`). **Rotacionar
> a senha PELO FLUXO DE AUTH DA APLICAÇÃO** (Recognition), nunca por shell/sudo/SQL direto, e **não commitar
> a nova senha em lugar nenhum**. Rotação automática **não** foi executada (exige a app rodando + interação de
> auth fora do escopo autônomo) → **pendência de gate humano, bloqueante para o go-live**. Ver §3 e §6-item-1.

---

## VEREDITO GO/NO-GO — ✅ **GO** (com pendências de sudo)

**Soak de 4.8h (288.6 min, 3398 amostras) com a stack completa co-residente: o Orin NX NÃO travou.**
O medo central — Postgres/Redis/API comendo a RAM unificada e matando a inferência — **não se
materializou**. Critérios:

| Critério GO | Alvo | Medido | Veredito |
|---|---|---|---|
| Duração contínua | ≥4h | **4.8h (288.6min)** | ✅ |
| Sem OOM | 0 kills | 0 (todos `Result=success`) | ✅ |
| RAM estável (sem leak) | slope ~0 | **−2.6 MB/h** (7181→7860MB, praticamente plano) | ✅ |
| Sem swap thrashing | — | swap fixo **2MB**, swap-out **0/s** o tempo todo | ✅ |
| Sem throttle térmico | <~99°C | GPU **max 67.3°C** | ✅ |
| API local E web respondendo | 100% | lat avg **9.7ms** / max 24.9ms; 28 cams o soak inteiro | ✅ |
| Cadência inferência | ≥5 inf/s/grupo | ~7.7 inf/s (31 fps/stream, interval=4); GPU 76% avg sustentado, 0 stream morto | ✅ |
| Recovery automático (kill) | systemd ressuscita | redis/infer ↺ em 6-8s | ✅ |
| Cold-start stack inteira (reboot-equivalente) | volta sozinho | **11s p/ 10/10 active, ~90s p/ steady-state** | ✅ |
| Reboot real do kernel | volta sozinho | **✅ PROVADO 2026-07-18** — reboot real de kernel: swap NVMe **persistiu**, `jetson-clocks` **reaplicou no boot** (serviço systemd), `swappiness=10`, e os **10 serviços `soak-*` subiram SOZINHOS** (verificado no box: `/swapfile 16G`, `swappiness=10`, `jetson-clocks.service` enabled+active) | ✅ |

**Overhead da camada de dados/API sobre a inferência: ~400MB** (redis 32 + pg 144 + api 125 + prod/cons 60).
RAM regime ~7.6GB de 15.6GB, ~8GB livres. **Cutover liberado** — o hardening de memória/clocks que era
pendência de sudo foi **aplicado e persistido** (swap NVMe 16GB + `swappiness=10` + `jetson-clocks` via systemd,
**verificados no box em 2026-07-18**). Restam: o **bloqueio de segurança da senha admin** (topo) e a **troca do
perfil de fan `quiet`→`cool`** antes da carga 24/7 (§6).

> **Achado do cold-start (valor de ouro):** o teste reboot-equivalente expôs um landmine latente — a
> instalação do `pgvector` havia **atualizado silenciosamente o Postgres 16→18** via conda-forge; o
> postmaster 16 seguiu servindo as 4.8h (subiu antes do upgrade), mas o restart lançou o binário 18
> contra data dir 16 → `database files are incompatible`. **Só apareceu no restart/reboot, não no soak.**
> Corrigido: removido pgvector + pinado `postgresql=16.14`; dados de embarque preservados; cold-start
> repetido → 10/10 OK. Lição incorporada em `REGRAS_PLATAFORMA_JETSON.md`.

---

## 1. O que subiu (stack completa co-residente)

Tudo como **`systemctl --user`** (Linger=yes → sobrevive a disconnect e reboot), `Restart=always`,
budget de memória e ordenação de OOM — **sem sudo** (ver `REGRAS_PLATAFORMA_JETSON.md` §3).

| Serviço (unit) | Papel | Budget (MemoryMax) | OOMScoreAdjust |
|---|---|---|---|
| `soak-postgres` | Postgres 16 (micromamba), shared_buffers 128MB, max_conn 30 | 900M | +300 |
| `soak-redis` | Redis 7.4 (source), maxmemory 512MB allkeys-lru | 640M | +500 |
| `soak-api` | Recognition API (gunicorn+gevent, py3.11) :8090 0.0.0.0 | 1300M | +400 |
| `soak-producer` | 28 cams × ~5 det/s → Redis (LPUSH+pubsub) + clipe 5MB/30s | 250M | +700 |
| `soak-consumer` | Redis → Postgres (batch INSERT) + queries dashboard 10s | 350M | +600 |
| `soak-sampler` | Telemetria 5s → `telemetry_soak.jsonl` | 150M | +800 |
| `soak-infer-epi` | deepstream 16 cams — PPE INT8 | (sem limite) | 0 (protegido) |
| `soak-infer-park` | deepstream 8 cams — YOLOX COCO | (sem limite) | 0 |
| `soak-infer-qaux` | deepstream 2 cams — PPE INT8 | (sem limite) | 0 |
| `soak-infer-qmain` | deepstream 2×4MP — RF-DETR ROI | (sem limite) | 0 |

Entrada: `mediamtx` + 5 pacers em tmux alimentam `rtsp://127.0.0.1:8554/cam0..27` (reuso da campanha).
Inferência headless (sink fakesink) para não depender de X.

## 2. Hardening de memória aplicado (com medição)

| Alavanca | Estado | Ganho / nota |
|---|---|---|
| Budgets por serviço (cgroup --user, sem sudo) | ✅ aplicado | teto agregado dos auxiliares ~3.6GB → garante folga p/ inferência+NVMM |
| Redis `maxmemory 512mb` + `allkeys-lru` | ✅ | evidência de evidência 5MB/30s testa evicção; RAM Redis estável ~26MB |
| Postgres edge (shared_buffers 128MB, max_conn 30, WAL 512MB) | ✅ | RSS PG ~160MB sob carga de INSERT contínuo |
| OOM ordering (auxiliares +300..+800, pipeline 0) | ✅ sem sudo | kernel mata auxiliar antes do pipeline DeepStream |
| `nvpmodel` 40W | ✅ (já estava) | — |
| **Swap NVMe (não-zram)** | ✅ **APLICADO + PERSISTIDO** (verificado no box 2026-07-18) | swapfile **NVMe 16GB** (`/swapfile`) substituiu o zram-only; persistiu no reboot real |
| `vm.swappiness` baixo | ✅ **APLICADO** (`/proc/sys/vm/swappiness`=10, era 60) | persistente via sysctl |
| `jetson_clocks` | ✅ **APLICADO + PERSISTIDO** | serviço systemd `jetson-clocks.service` **enabled+active** — reaplica no boot |
| **Perfil de fan** | ⚠️ **`quiet` temporário** — reverter p/ `cool` antes do cutover | carga 24/7 dos 3 módulos exige `cool` (§6-item-2, checklist task-097) |

## 3. Embarque RVB (vínculo sistema ↔ edge) — ✅ LOCAL e WEB validados

- **Tenant** `rvb` (`bb6757d9-…`) já existia (migrations) → `deployment_mode=hybrid` (dual: edge+cloud).
- **Admin** criado: `admin@rvb.com.br`. 🔴 **A senha do embarque foi commitada no git** (via `RVB_ADMIN_PASSWORD`) → **rotacionar pelo fluxo de auth da APLICAÇÃO** antes do go-live (nunca por shell/SQL; não commitar a nova). **Não rotacionada** — bloqueio de segurança aberto (banner do topo + §6-item-1). Login OK, JWT com `tenant_schema=rvb`, `role=admin`, `modules=[epi,counting,quality]`.
- **3 models** em `rvb.models` (epi/quality/counting, active).
- **28 câmeras** cadastradas em `rvb.cameras` **e** `public.cameras`, com `active_module` +
  `model_<módulo>_id`, ligadas aos streams paced (`rtsp://127.0.0.1:8554/camN`):
  - EPI/Segurança: 16 · Estacionamento/Carga (counting): 8 · Qualidade: 4.
- **Acesso LOCAL (LAN):** `http://127.0.0.1:8090` — login + `/api/cameras` → **28** ✓
- **Acesso WEB (Tailscale/túnel):** `http://100.93.126.76:8090` — health + login + `/api/cameras` → **28** ✓
- Frontend web UI (Vite build) não subiu neste soak (API-only mode) — pendência de baixo custo.

## 4. Chaos / recovery

| Teste | Resultado |
|---|---|
| SIGKILL `soak-redis` | ✅ ressuscitado por systemd em ~6s (PONG), `NRestarts=1` |
| SIGKILL `soak-infer-epi` | ✅ ressuscitado em ~8s (reload engine INT8), `NRestarts=1` |
| Cold-start da stack inteira (stop→start, reboot-equivalente) | ✅ **10/10 active em 11s, steady-state ~90s** (gunicorn + 28 RTSP reconnect). Expôs+corrigiu o landmine PG16→18 (ver Veredito) |
| Reboot completo do kernel | ✅ **PROVADO 2026-07-18** — reboot real: **10/10 `soak-*` voltaram sozinhos** (Linger=yes + units enabled), swap NVMe persistiu, `jetson-clocks` reaplicado no boot, `swappiness=10` mantido. **Gate de recovery de cutover = provado.** |

## 5. Telemetria do soak (4.8h, 3398 amostras @ 5s)

Sampler independente → `~/soak113/logs/telemetry_soak.jsonl`. Sumário: `python3 ~/soak113/scripts/soak_report.py`.

| Métrica | min | avg | max | Nota |
|---|---|---|---|---|
| RAM usada (MB) | 7181 | 7829 | 7860 | **slope −2.6 MB/h → sem leak** |
| RAM disponível (MB) | 7796 | 7827 | 8474 | ~8GB livres constante |
| Swap usado (MB) | 2 | 2 | 2 | plano |
| Swap-out (pág/s) | 0 | 0 | 0 | **sem thrashing** |
| GPU load (%) | 0 | 75.8 | 99.7 | inferência contínua |
| GPU temp (°C) | 64.0 | 66.0 | **67.3** | sem throttle |
| VDD_IN (W) | 16.0 | 20.9 | 23.2 | dentro do envelope 40W |
| Latência API (ms) | 6.1 | 9.7 | 24.9 | responsiva o soak inteiro |
| loadavg(1m) | 4.6 | 8.0 | 13.6 | 8 cores — utilização alta, sem saturar |

**PSI indisponível neste kernel** (sem `CONFIG_PSI`) — pressão de memória inferida por swap-out/MemAvailable
(ambos saudáveis). **dmesg restrito** (sem sudo) — OOM inferido por `systemd Result`/`NRestarts` (zero OOM).

## 6. Pendências que exigem o Vitor (sudo/credenciais)

**Fechadas em 2026-07-18** (hardening aplicado + persistido, **verificado no box** — ver §2/§4/Veredito):
- ~~Swap NVMe + `vm.swappiness=10`~~ → ✅ swapfile NVMe 16GB + `swappiness=10`.
- ~~`jetson_clocks`~~ → ✅ serviço systemd `jetson-clocks.service` (enabled+active).
- ~~Reboot completo p/ provar recovery~~ → ✅ reboot real: 10/10 `soak-*` voltaram sozinhos.

**Abertas:**
1. 🔴 **Rotação da senha do admin `admin@rvb.com.br`** (commitada no git no embarque) — **pelo fluxo de auth da
   app**, não por shell/SQL; **não commitar a nova**. **Bloqueante para o go-live** (banner topo, §3).
2. ⚠️ **Perfil de fan `quiet` → `cool`** antes da carga 24/7 (checklist pré-embarque da task-097, sudo = Vitor):
   ```bash
   sudo sed -i 's/FAN_DEFAULT_PROFILE .*/FAN_DEFAULT_PROFILE cool/' /etc/nvfancontrol.conf && sudo systemctl restart nvfancontrol
   ```
3. **pgvector p/ PG16** (assistente de treino) — mismatch PG18; fora do caminho crítico RVB.
4. **Frontend web build** (Vite) para UI completa via túnel (API já responde os dois caminhos).
5. Docker group (opcional) se quiser containers no futuro.

## 7. Reprodução / operação
```bash
ssh pandora@100.93.126.76
systemctl --user list-units 'soak-*'            # estado
python3 ~/soak113/scripts/soak_report.py         # sumário telemetria
tail -f ~/soak113/logs/telemetry_soak.jsonl      # ao vivo
# parar tudo: systemctl --user stop 'soak-*'
```
