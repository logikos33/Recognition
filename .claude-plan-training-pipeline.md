# PLANO — Pipeline de Treinamento MLOps (feat/training-pipeline-mlops)

> Fonte única de verdade para os agentes executores. Baseado em mapeamento exaustivo do develop
> (f0a8ce2) + specs: ADR-0031/0032/0033/0034, runbook TRAINING_PIPELINE_WEEKEND_MVP.md, task-054/055.
> License-safe: RF-DETR/YOLOX (Apache), onnxruntime (MIT). ZERO ultralytics no serving.

## Fatos verificados (NÃO re-derivar; NÃO confiar em DATABASE.md — está stale)

- Migrations canônicas: `infra/migrations/NNN_nome.sql`. Última: 092. **Próxima: 093.**
  Runner de produção: `railway_start.py` inline (roda TODOS os .sql em ordem no boot, engole
  "already exists", rollback do ARQUIVO inteiro no primeiro erro) → **cada statement deve ser
  individualmente idempotente**; harness CI roda tudo 2x (`tests/harness/migrations/runner.py`).
- Tabelas existentes (extends, não recria):
  - `yolo_classes` (id SERIAL, user_id NOT NULL, name, color, UNIQUE(user_id,name)) — vira "classes".
  - `training_frames` (id, video_id NOT NULL FK, frame_number, filename, timestamp_seconds,
    is_annotated, module_code, tenant_id, pre_annotations JSONB, pre_annotated_at,
    uncertainty_score, priority_rank, quality_status, quality_scores, validated_by, validated_at)
    — vira "training_images". `filename` É a chave R2 (`frames/{user_id}/{video_id}/frame_NNNN.jpg`).
  - `frame_annotations` (frame_id FK, class_id FK yolo_classes, x_center/y_center/width/height
    normalizados 0..1 com CHECKs) — vira "annotations". **Manter formato de colunas** (contrato do
    AnnotationInterface.jsx); bbox JSONB da spec = representado por essas 4 colunas.
  - `dataset_versions` (id, user_id, version VARCHAR, frame_count, train/val/test_count,
    class_distribution JSONB, metadata_key) — SEM tenant_id/module_code/parent. Nunca é escrita
    pelo task versioning (bug conhecido).
  - `training_jobs` (id, user_id, preset CHECK, model_size **SEM CHECK** default 'yolov8n', status
    CHECK(pending|running|completed|failed|stopped), progress, current_epoch, total_epochs,
    metrics JSONB, error_message, progress_file, started_at, **completed_at** (não finished_at),
    tenant_id via migration 005).
  - `trained_models` (id, user_id, job_id FK **sem UNIQUE**, name, model_path, map50, precision,
    recall, is_active, scenario_config JSONB (052), created_by/origin/tenant_id (090)).
  - `public.integrations` (082): credenciais por tenant, `integration_type` inclui 'vast_ai',
    secret Fernet com `CAMERA_SECRET_KEY` — usar para VAST_API_KEY por tenant, fallback env.
  - `public.cameras`: manufacturer/host/port/username/password_encrypted/channel/subtype +
    model_epi_id/model_quality_id/model_counting_id + site_id.
  - `module_classes` (module_code, class_name, dino_prompt, is_violation, color) — catálogo de
    módulo, NÃO confundir com classes do tenant.
- AUSENTES (criar): `recorders`, `datasets` (pai), `model_deployments`, `model_evaluations`,
  `model_drift_metrics`.
- Padrões obrigatórios: BaseRepository (_execute/_execute_one/_execute_mutation/
  _execute_in_transaction) + DatabasePool.get_instance(); success()/error() de app.core.responses;
  get_tenant_id()/get_current_user_id() de app.core.auth; Fernet(CAMERA_SECRET_KEY) p/ segredos
  (padrão camera_service._encrypt_password); storage via get_storage() (NUNCA R2Storage() direto);
  Celery @celery.task(bind=True, queue='...', name='tasks.x.y'); RTSPUrlValidator + SSRF gate
  (probe_handler._resolve_and_pin) antes de URL chegar a ffmpeg/requests.
- Celery: queues extraction/versioning/training/inference/quality_* — usar existentes. Progresso:
  Redis SETEX+PUBLISH `training:{job_id}` → socket_bridge → SocketIO /training (`training_progress`).
- CI (comandos exatos):
  - `ruff check services/api/` (config services/api/; select E,W,F,I,N,UP,B,SIM,S; line 100; py311)
  - `pytest services/api/tests/ -v --tb=short -q --cov=app --cov-report=term-missing
    --cov-fail-under=60` (DATABASE_URL/REDIS_URL/JWT_SECRET_KEY/SERVICE_TYPE=api de CI)
  - harness: `python tests/harness/migrations/runner.py --pass 1` e `--pass 2`
    (HARNESS_DATABASE_URL, pgvector/pg15)
  - license gate: `scripts/check_license_gate.py` + `scripts/scan_requirements_licenses.py`
    (AGPL proibido em requirements/{base,api,worker,inference,celery-worker}.txt; training.txt é
    isento)
- Bugs pré-existentes a corrigir (falha-antes/passa-depois):
  1. `auto_training.py`: usa `finished_at` (não existe; é completed_at) E chama
     `dispatch_training.delay(tenant_id)` com 1 arg (assinatura exige job_id + dataset_version_id)
     → beat nunca funcionou.
  2. `versioning.py`: nunca INSERTa em dataset_versions; e monta src key
     `frames/{user_id}/{frame_id}/{filename}` ≠ real `frames/{user_id}/{video_id}/frame_NNNN.jpg`
     → copy silenciosamente falha (dataset vazio).
  3. `pre-annotation-service/src/services/active_learning.py`: `WHERE status='pre_annotated'` —
     coluna não existe (é quality_status / pre_annotations IS NOT NULL).
- PORT de staging→feature (develop NÃO tem; staging tem): detectors ONNX
  (`services/api/app/domain/detectors/*` — base/factory/onnx_yolox/onnx_rfdetr/ultralytics_compat),
  `training/vast/*` (provision_and_train.sh, train_yolox.py, train_rfdetr.py, upload_and_register.py),
  `scripts/scan_requirements_licenses.py`, job de license extra no ci.yml, versão ONNX de
  `tasks/inference.py`, `services/inference/*` (detectors.py, engine, config, requirements).
  CONFLITO conhecido: `tasks/training.py` — develop tem propagação created_by/origin/tenant_id
  (9650dd3); staging tem _dispatch_vast_ai real-ish. Mesclar à mão preservando ambos.

## Migrations (093–101) — todas: statements individualmente idempotentes, tenant_id, rodar 2x

- **093_classes_tenant_module.sql**: yolo_classes ADD tenant_id UUID REFERENCES tenants(id),
  module_code VARCHAR(50) DEFAULT 'epi'; backfill tenant_id FROM users; INDEX (tenant_id, module_code).
- **094_training_images_sources.sql**: training_frames ADD source VARCHAR(20) DEFAULT 'video'
  (valores: auto|nvr|upload|video — CHECK via DO $$ guardado), camera_id UUID REFERENCES cameras(id),
  recorder_id UUID, r2_key TEXT, captured_at TIMESTAMPTZ, model_confidence DOUBLE PRECISION,
  width INTEGER, height INTEGER; backfill r2_key = filename WHERE r2_key IS NULL;
  DO $$ ALTER COLUMN video_id DROP NOT NULL (guardado; não-destrutivo — justificar no ADR);
  INDEX (tenant_id, source), (tenant_id, is_annotated).
  Obs: status da spec (unlabeled|labeled|reviewed) é COMPUTADO no SELECT
  (unlabeled = NOT is_annotated; labeled = is_annotated AND validated_at IS NULL;
  reviewed = validated_at IS NOT NULL) — sem coluna nova (evita dual-write).
- **095_annotations_provenance.sql**: frame_annotations ADD source VARCHAR(20) DEFAULT 'manual'
  (manual|pre_annotation), created_by UUID, reviewed_by UUID.
- **096_datasets_parent.sql**: CREATE datasets (id UUID PK gen_random_uuid(), tenant_id UUID NOT
  NULL REFERENCES tenants(id), module_code VARCHAR(50) DEFAULT 'epi', name VARCHAR(255) NOT NULL,
  description TEXT, created_by UUID, created_at TIMESTAMPTZ DEFAULT NOW());
  dataset_versions ADD tenant_id UUID REFERENCES tenants(id) (backfill via users), module_code,
  dataset_id UUID REFERENCES datasets(id), split JSONB, augmentations JSONB, coco_r2_key TEXT,
  export_format VARCHAR(20), status VARCHAR(20) DEFAULT 'ready', created_by UUID; INDEXes.
- **097_training_jobs_pipeline.sql**: training_jobs ADD dataset_version_id UUID REFERENCES
  dataset_versions(id), framework VARCHAR(20) DEFAULT 'rfdetr', base_model VARCHAR(50),
  hyperparams JSONB DEFAULT '{}', gpu_provider VARCHAR(20), gpu_instance_ref TEXT; backfill
  tenant_id FROM users WHERE NULL (coluna já existe via 005); INDEX (tenant_id, status).
- **098_models_registry_lineage.sql**: trained_models ADD framework VARCHAR(20),
  r2_onnx_key TEXT, r2_weights_key TEXT, metrics JSONB DEFAULT '{}', dataset_version_id UUID
  REFERENCES dataset_versions(id), module_code VARCHAR(50) DEFAULT 'epi';
  INDEX (tenant_id, module_code, is_active); INDEX em job_id.
- **099_recorders.sql**: CREATE recorders (id UUID PK, tenant_id UUID NOT NULL REFERENCES
  tenants(id), name VARCHAR(255) NOT NULL, host VARCHAR(255) NOT NULL, port INTEGER DEFAULT 80,
  username VARCHAR(255), password_encrypted TEXT, protocol VARCHAR(20) DEFAULT 'onvif',
  manufacturer VARCHAR(50), channels INTEGER DEFAULT 0, retention_days INTEGER,
  status VARCHAR(20) DEFAULT 'unknown', last_tested_at TIMESTAMPTZ, last_error TEXT,
  created_by UUID, created_at TIMESTAMPTZ DEFAULT NOW()); CHECK protocol via DO $$;
  INDEX (tenant_id).
- **100_model_deployments.sql**: CREATE model_deployments (id UUID PK, tenant_id UUID NOT NULL
  REFERENCES tenants(id), model_id UUID NOT NULL REFERENCES trained_models(id), camera_id UUID
  NOT NULL REFERENCES cameras(id), module_code VARCHAR(50) DEFAULT 'epi', config JSONB DEFAULT
  '{}', status VARCHAR(20) DEFAULT 'active', deployed_by UUID, created_at TIMESTAMPTZ DEFAULT
  NOW(), deactivated_at TIMESTAMPTZ); INDEX (tenant_id, camera_id, module_code, status),
  (model_id).
- **101_model_eval_drift.sql**: CREATE model_evaluations (id UUID PK, tenant_id UUID NOT NULL
  REFERENCES tenants(id), model_id UUID REFERENCES trained_models(id), champion_model_id UUID
  REFERENCES trained_models(id), dataset_version_id UUID REFERENCES dataset_versions(id),
  metrics JSONB DEFAULT '{}', confusion_matrix JSONB, verdict VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMPTZ DEFAULT NOW());
  CREATE model_drift_metrics (id UUID PK, tenant_id UUID NOT NULL REFERENCES tenants(id),
  model_id UUID REFERENCES trained_models(id), camera_id UUID REFERENCES cameras(id),
  window_start TIMESTAMPTZ, window_end TIMESTAMPTZ, detections_count INTEGER DEFAULT 0,
  avg_confidence DOUBLE PRECISION, class_distribution JSONB, drift_score DOUBLE PRECISION,
  created_at TIMESTAMPTZ DEFAULT NOW()); INDEXes.

Regra: commits de migration SEPARADOS de commits de lógica (atômico por par
migration → model/repo/service/route/test na sequência).

## Workstreams de código

### WS-0 PORT (eu, orquestrador): detectors + vast scripts + license CI + inference ONNX (staging → branch)
### WS-A1 Classes: TenantClassService — queries por tenant_id (fallback user_id p/ legado);
  GET/POST existentes em /api/classes + NOVOS PUT /api/classes/<id>, DELETE /api/classes/<id>
  (409 se anotações referenciam); campo module_code; contagem de amostras por classe
  (GET /api/classes?include_counts=true).
### WS-A2 Ingestão upload: POST /api/training/images/upload (multipart batch) → R2
  `training-images/{tenant_id}/upload/{uuid}.jpg` (novo R2Prefix.TRAINING_IMAGES) →
  training_frames source='upload' (video_id NULL, width/height de PIL). GET /api/training/images:
  filtros source= e status= (computado) + manter compat. Extração de vídeo: JÁ existe
  (POST /api/training/videos + extract_frames) — apenas marcar source='video' no create.
### WS-A3 Datasets: DatasetService (tenant-scoped) + rotas: GET/POST /api/v1/datasets;
  POST /api/v1/datasets/<id>/versions {name?, split{train,val,test}, augmentations?, format}
  → Celery build_dataset_version_v2 (queue versioning): snapshot frames rotulados (reviewed
  primeiro), split por grupo de vídeo (sem leakage; frames soltos por hash), gera COCO JSON
  (converter YOLO cx/cy/w/h→COCO absoluto via width/height; fallback lê imagem do R2), upload
  `datasets/{tenant_id}/{dataset_id}/v{N}/...` + `_annotations.coco.json` por split, INSERT
  dataset_versions (linhagem completa) — CORRIGE bug do versioning.py atual (key mismatch e
  no-insert; task antiga permanece p/ compat, nova é a oficial).
  GET /api/v1/dataset-versions/<id> (detalhe + presigned do COCO).
### WS-A4 Vast.ai real: infrastructure/gpu/vast_client.py (REST https://console.vast.ai/api/v0:
  search offers GPU RTX 4090/3090 preço-cap, create instance com onstart script, get status,
  destroy). tasks/training.py: _dispatch_vast_ai REAL: resolve key (integrations vast_ai →
  env VAST_API_KEY); gera presigned GET do dataset COCO + presigned PUT dos artefatos
  (onnx/pesos/metrics.json); onstart baixa `training/vast/remote_train.py` (novo runner
  self-contained: pip install rfdetr|yolox, treina de dataset_version, exporta ONNX, valida
  onnxruntime, sobe artefatos, POSTa progresso). Progresso: script POSTa em
  POST /api/v1/training/jobs/<id>/progress-callback (auth: token HMAC job-scoped
  JWT_SECRET_KEY-derived, gerado no dispatch, passado via env da instância) → update DB +
  Redis publish training:{job_id} (bridge existente). Poll de instância como watchdog +
  timeout; stop → destroy instance. Fallback sem key: _simulate_training existente +
  status log 'PENDENCIA: VAST_API_KEY'. max_retries/countdown ajustados p/ provisioning.
### WS-A5 Registry+Models API: ModelRegistry em trained_models (decisão: registry canônico =
  public.trained_models; {schema}.models/ModelRolloutRepository permanece intocado p/ pin/canary).
  Registro pós-treino: callback final do job cria trained_models row (framework, r2_onnx_key,
  r2_weights_key, metrics por classe, dataset_version_id, job_id, tenant_id, origin='vast_ai').
  Task validate_onnx (worker, onnxruntime session + dummy input) antes de marcar utilizável.
  Rotas novas (blueprint models_registry, url_prefix /api/v1):
  GET /api/v1/models (lista tenant, filtros module/status), GET /api/v1/models/<id> (linhagem
  expandida: dataset_version→job→model + deployments), POST /api/v1/models/<id>/activate
  (gate de eval; substitui/encadeia com activate legado), GET /api/v1/models/<id>/eval,
  GET /api/v1/models/<id>/drift.
### WS-A6 Inferência ONNX por câmera: tasks/inference.py resolve modelo efetivo da câmera
  (model_deployments ativo → cameras.model_epi_id → trained_models.r2_onnx_key → download /tmp
  cache → get_detector(framework→backend map)); fallback DETECTOR_MODEL_PATH. Publica
  camera:model_change já existente.
### WS-B1 Recorders (ADR-0034): RecorderRepository/Service (Fernet), blueprint /api/v1/recorders:
  GET/POST/PUT/DELETE; POST /api/v1/recorders/<id>/test (conectividade);
  GET /api/v1/recorders/<id>/recordings?camera=&from=&to= → timeline {segments:[{start,end}],
  gaps:[...]} via cadeia: ONVIF Profile G (onvif-zeep, lazy import) → Hikvision ISAPI
  (POST /ISAPI/ContentMgmt/search, requests+DigestAuth, XML) → Dahua/Intelbras
  (mediaFileFind HTTP) → RTSP probe fallback (informar capacidade). SSRF gate em host/port.
  POST /api/v1/recorders/<id>/extract-frames {camera_channel, from, to, interval_seconds,
  max_frames} → Celery nvr_extract_frames (queue extraction): playback URL por protocolo
  (Hik: rtsp://host:554/Streaming/tracks/{ch}01?starttime=YYYYMMDDTHHMMSSZ&endtime=...;
  Dahua: rtsp://host/cam/playback?channel={n}&starttime=...; ONVIF: GetReplayUri),
  RTSPUrlValidator, ffmpeg -rtsp_transport tcp -i URL -vf fps=1/N → R2
  training-images/{tenant_id}/nvr/{recorder_id}/... → training_frames source='nvr'.
  Edge: se site tiver gateway → criar edge_command 'nvr_extract' (gancho; agente edge
  incompleto → PENDÊNCIA documentada), senão pull pela rede.
### WS-B2 Active learning: uncertainty = 1 - |2*conf - 1| no ingest auto; GET
  /api/training/active-learning-queue?limit=&module= → não-anotadas ORDER BY priority_rank
  NULLS LAST, uncertainty_score DESC NULLS LAST; Celery score_active_learning (batch re-rank);
  fix bug active_learning.py do pre-annotation-service.
### WS-B3 Auto-captura: hook em tasks/inference.py::_save_alert + socket_bridge::
  _create_alert_and_verify — gated (env AUTO_CAPTURE_ENABLED default true + janela de confiança
  [0.30,0.85] + rate-limit Redis 1/min/câmera) → training_frames source='auto',
  r2_key=evidence_key, model_confidence, uncertainty, tenant_id/module_code da câmera.
  POST /api/v1/alerts/<id>/add-to-training (explícito, sem rate-limit).
  Corrigir AlertRepository.create p/ gravar tenant_id+module_code (bug conhecido).
### WS-B4 Pré-anotação: frames_bp (vazio) ganha: POST /api/frames/<id>/pre-annotate (proxy
  pre-annotation-service, PRE_ANNOTATION_SERVICE_URL), POST /api/frames/pre-annotate/batch
  (Celery pre_annotate_batch → HTTP batch máx 50); POST /api/frames/<id>/annotations/accept-
  suggestions (pre_annotations JSONB → frame_annotations source='pre_annotation').
  Aliases spec: GET/POST/PUT /api/frames/<id>/annotations → handlers existentes.
### WS-C1 Eval campeão×desafiante: Celery evaluate_model (queue training): baixa ONNX
  challenger+champion + split test do dataset_version (COCO do R2) → roda detector factory →
  matching greedy IoU@0.5 → P/R/AP por classe + mAP + matriz de confusão (numpy puro) →
  INSERT model_evaluations com verdict (promote se mAP_challenger >= mAP_champion - 0.01 e
  nenhuma classe crítica cai >5pp; sem champion → promote). POST /api/v1/models/<id>/evaluate
  (dispara), GET /api/v1/models/<id>/eval (última). Gate no activate: verdict=reject → 409
  (override force=true exige admin).
### WS-C2 Deploy/model-config: POST /api/cameras/<id>/model-config {model_id, config{roi[],
  line[], classes[], thresholds{}}} → model_deployments upsert (desativa anterior ativo da
  câmera+módulo) + cameras.model_{module}_id sync + Redis camera:model_change.
  GET /api/cameras/<id>/model-config → deployment ativo. POST /api/cameras/<id>/model-config/
  rollback → reativa deployment anterior. Geometrias normalizadas 0..1 (ADR-0032), validação
  server-side (roi ≥3 pts, line =2 pts, thresholds por classe 0..1).
### WS-C3 Drift/monitor: Celery beat compute_drift_metrics (diário, queue training): agrega
  alerts por câmera×modelo (janela 24h): count, avg_confidence, class_distribution; baseline =
  primeira janela do deployment; drift_score = |avg_conf - baseline| + L1 da distribuição;
  se > threshold → INSERT sugestão (log + gancho check_auto_retraining). GET /api/v1/models/
  <id>/drift. Stub honesto, gancho pronto.
### WS-C4 Fix auto_training.py: completed_at; criar job row antes; .delay(job_id,
  dataset_version_id) com última dataset_version do tenant; teste falha-antes/passa-depois.
### WS-D Permissões: decorator require_training_role(level) em app/core/auth.py:
  'write' (criar/rotular/versionar/treinar) → role em (admin, superadmin) ou override
  training:write em user_permission_overrides; 'approve' (activate/deploy/rollback) → admin+
  ou override training:approve. Aplicar nos endpoints novos de mutação (não quebrar rotas
  existentes de leitura). Documentar no ADR.
### WS-E Docs: ADR-0037 (contrato de API — MAPA completo), ADR-0038 (Vast.ai provisioning +
  callback + fallback), docs/architecture/TRAINING_PIPELINE_DESIGN.md (flywheel 11 estágios
  como implementado — o doc citado que não existia), atualizar docs/DATABASE.md (seção nova),
  registrar licença dataset em docs/datasets/PPE_LICENSE.md (bootstrap Roboflow CC BY 4.0).

## Endpoints — MAPA DO CONTRATO (resumo; detalhar no ADR-0037)

Envelope sempre {"status":"success|error","data":{...}} (padrão success()/error() do repo — na
prática {'success': bool, ...}; documentar formato REAL no ADR).

| Método | Path | Novo? |
|---|---|---|
| GET/POST | /api/classes | existente (+tenant/module) |
| PUT/DELETE | /api/classes/<id> | NOVO |
| GET | /api/training/images?source=&status=&page= | estendido |
| POST | /api/training/images/upload | NOVO |
| GET | /api/training/active-learning-queue | NOVO |
| GET/POST | /api/training/frames/<id>/annotations | existente (canônico) |
| POST | /api/frames/<id>/pre-annotate (+/batch, accept-suggestions) | NOVO |
| GET/POST | /api/v1/datasets | NOVO |
| POST | /api/v1/datasets/<id>/versions | NOVO |
| GET | /api/v1/dataset-versions/<id> | NOVO |
| POST | /api/training/jobs {dataset_version_id, framework, base_model, hyperparams} | estendido |
| GET | /api/training/jobs/<id>/status·progress | existente |
| POST | /api/training/jobs/<id>/stop | existente (destrói instância GPU) |
| POST | /api/v1/training/jobs/<id>/progress-callback | NOVO (interno GPU→API) |
| GET | /api/v1/models · /api/v1/models/<id> | NOVO |
| POST | /api/v1/models/<id>/activate · /evaluate | NOVO |
| GET | /api/v1/models/<id>/eval · /drift | NOVO |
| CRUD | /api/v1/recorders (+test) | NOVO |
| GET | /api/v1/recorders/<id>/recordings?camera=&from=&to= | NOVO |
| POST | /api/v1/recorders/<id>/extract-frames | NOVO |
| GET/POST | /api/cameras/<id>/model-config (+rollback) | NOVO |
| POST | /api/v1/alerts/<id>/add-to-training | NOVO |

## Gates
ruff verde; pytest cov≥60 (comandos exatos CI); harness migrations 2x; license gate sem AGPL
(rfdetr/yolox APENAS em requirements/training.txt e no runner remoto da GPU — nunca em
api/worker/inference); testes falha-antes/passa-depois dos 3 bugs; commits convencionais
(migration separada de lógica); push origin feat/training-pipeline-mlops; PR → develop; NÃO
mergear (gate humano).

## AJUSTES DA REVISÃO ARQUITETURAL (Opus — vinculantes)

1. **Auto-capture SÓ em `_save_alert()`** (tasks/inference.py). NÃO no socket_bridge (captura dupla +
   TOCTOU). Rate-limit com Redis `SET key NX EX 60` atômico + teto `MAX_AUTO_CAPTURE_PER_DAY_PER_CAMERA`
   (env, default 200). Bridge alerts → só via POST /api/v1/alerts/<id>/add-to-training.
2. **Canal Redis: MANTER `training_progress:{job_id}`** no Celery (o bridge psubscribe `training:*`
   NÃO deve receber — evita `_register_trained_model` duplicar row; job_id não tem UNIQUE).
   Registro de modelo: EXCLUSIVO do fluxo Celery/callback via TrainingRepository com guarda
   (get_model_by_job_id antes de INSERT). Opcional: bridge psubscribe `training_progress:*` SÓ para
   emitir socketio (sem registro).
3. **frame_repository.create/create_bulk**: `video_id: Optional[UUID]` + novos params (source, r2_key,
   camera_id, recorder_id, width, height, model_confidence, captured_at, tenant_id, module_code) —
   ANTES da 094 ser usada. extraction.py passa width/height de frame.shape.
4. **build_dataset_version_v2**: LEFT JOIN (frames com video_id NULL entram; split por grupo = video_id
   ou hash do frame id). Corrigir key mismatch (video_id no path, não frame_id).
5. **Callback token**: coluna `callback_token` em training_jobs (097), token randômico por job
   (secrets.token_urlsafe), revogado no stop; validação payload (0≤progress≤100, metrics dict,
   error_message ≤500 chars); limiter 60/min no endpoint.
6. **Registry routes no blueprint EXISTENTE** services/api/app/api/v1/models/ (models_rollout_bp).
   GET /api/training/models ganha header `Deprecation: true` + nota ADR.
7. **Activate sincroniza {schema}.models**: handler chama ModelRolloutRepository.pin_model()
   best-effort (schema ausente → log warning, segue). Documentar ADR-0037.
8. **AlertRepository.create**: params opcionais tenant_id/module_code (retrocompat); atualizar
   inference.py e socket_bridge (raw SQL) juntos.
9. **DatasetRepository**: método novo create_version_v2 com colunas da 096.
10. **training.py refatorado usa TrainingRepository** (não AnnotationRepository).
11. **Backfill width/height**: script manual env-gated `scripts/backfill_frame_dimensions.py`
    (NUNCA migration — seed/backfill de dados pesados fora do boot). Exporter COCO: fallback R2
    com ThreadPoolExecutor(10).
12. **3 PRs empilhados** (gate humano em cada):
    - PR-1 `feat/tp1-schema-port-fixes`: migrations 093–101 + port staging (detectors/vast/license) +
      updates de repos + 3 bugfixes com testes falha-antes/passa-depois.
    - PR-2 `feat/tp2-ingestion-training` (base PR-1): WS-A1..A6 + WS-B1..B4.
    - PR-3 `feat/tp3-eval-deploy-drift` (base PR-2): WS-C1..C4 + WS-D + WS-E.
13. Pós-deploy: verificar colunas aplicadas (runner do railway_start não aborta em erro real) —
    documentar query information_schema no runbook.

## Pendências conhecidas (reportar no final)
- VAST_API_KEY não configurada no ambiente → dispatch real pronto, não exercitado end-to-end.
- Edge agent incompleto → extração NVR no edge fica como edge_command gancho.
- docs/architecture/TRAINING_PIPELINE_DESIGN.md não existia — criado nesta entrega.
- Treino E2E real (weekend) exige conta Vast.ai + dataset Roboflow baixado.
